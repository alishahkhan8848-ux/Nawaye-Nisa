rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Each user can only read/write files under their own folder.
    // Evidence uploads live at users/{uid}/evidence/*.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
